@model Project.Web.Models.ComponentDetailsViewModel

@{
    ViewBag.Title = "ProductDetailComponents";
}

<h2>ProductDetailComponents</h2>

<p>
    @Html.ActionLink("Create New", "CreateComponentPart", new { id = Model.Component.Id} )
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Component.Name)
        </th>
    </tr>

@foreach (var item in Model.Component.ComponentParts {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
